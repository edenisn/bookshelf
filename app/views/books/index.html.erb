<table class="table table-hover">
  <thead>
    <tr>
      <th>Name</th>
      <th>ISBN</th>
    </tr>
  </thead>

  <% @books.each do |book| %>
    <tbody>
      <tr>
        <td><%= book.name %></td>
        <td><%= book.isbn %></td>
      </tr>
    </tbody>
  <% end %>
</table>
